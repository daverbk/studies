name: Deploy Notes

on:
  workflow_dispatch:
  push:
    branches:
      - 'main'
    paths:
      - 'notes/**'

env:
  notes-dir: ./notes

jobs:
  build:
    name: Build Docusaurus
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: npm
          cache-dependency-path: ${{ env.notes-dir }}/package-lock.json

      - name: Install dependencies
        run: |
          cd ${{ env.notes-dir }}
          npm install

      - name: Build website
        run: |
          cd ${{ env.notes-dir }}
          npm run build

      - name: Upload Build Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ${{ env.notes-dir }}/build

  deploy:
    name: Deploy to GitHub Pages
    needs: build

    permissions:
      pages: write
      id-token: write

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
